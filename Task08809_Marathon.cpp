#include <iostream>

int main()
{
    int64_t a, b;
    std::cin >> a >> b;
    std::cout << a - b << std::endl;

    return 0;
}

/*The running competitions was started a by participants, 
but b of them left the race. How many runners finished?*/