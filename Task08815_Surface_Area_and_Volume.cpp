#include <iostream>

int main()
{
    int64_t a;
    std::cin >> a;
    std::cout << 6 * a * a << " " << a * a * a << std::endl;

    return 0;
}

/*Find the surface area and volume of the cube by the length of its edge*/