#include <iostream>

int main()
{
    int64_t a, b, c;
    std::cin >> a >> b >> c;
    std::cout << (a - c) + (b - c) + c << std::endl;

    return 0;
}

/*At the school concert a students sang, b students danced, 
and c students sang and danced. How many participants were there in total?*/